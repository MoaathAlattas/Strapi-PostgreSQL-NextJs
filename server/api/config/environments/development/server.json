{
  "host": "0.0.0.0",
  "port": "${process.env.API_PORT}",
  "proxy": {
    "enabled": "${process.env.API_PROXY_ENABLED}",
    "host": "${process.env.API_HOST}",
    "port": "${process.env.API_PORT}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}