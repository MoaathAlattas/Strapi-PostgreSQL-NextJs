{
  "host": "0.0.0.0",
  "port": 1337,
  "proxy": {
    "enabled": true,
    "host": "${process.env.API_URL}",
    "port": "${process.env.APP_PORT}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}