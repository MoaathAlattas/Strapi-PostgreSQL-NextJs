{
  "host": "0.0.0.0",
  "port": "${process.env.APP_PORT}",
  "proxy": {
    "enabled": true,
    "host": "${process.env.API_URL}",
    "port": "${process.env.APP_PORT}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}